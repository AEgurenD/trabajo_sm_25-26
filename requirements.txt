# ── Núcleo científico ──
numpy>=1.26,<3
scipy>=1.11
matplotlib>=3.8

# ── Audio I/O ──
# OGG (Vorbis) y FLAC de forma nativa, sin ffmpeg ni dependencias externas
soundfile>=0.12

# ── GUI Qt ──
PySide6>=6.6

# ── Renderizado de gráficas en UI (matplotlib → QLabel via cv2) ──
opencv-python>=4.8

# ── PyTorch (necesario para EnCodec) ──
# CPU por defecto. Para CUDA:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1

# ── Compresión neuronal ──
# Solo EnCodec (Meta). DAC no es compatible con Python >= 3.13.
encodec>=0.1.1